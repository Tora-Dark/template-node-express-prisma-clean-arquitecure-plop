import { Expose, plainToClass } from 'class-transformer';

export class {{pascalCase name}}ResponseDTO {
  @Expose()
  name!: string;

  @Expose()
  lastname!: string;

  @Expose()
  age!: number;

  @Expose({ name: 'fullName' })
  getFullName() {
    return `${this.name} ${this.lastname}`; // Adjust logic as necessary
  }

  // Method to transform plain object into {{pascalCase name}}ResponseDTO instance
  static fromRaw(rawData: object): {{pascalCase name}}ResponseDTO {
    return plainToClass({{pascalCase name}}ResponseDTO, rawData, {
      excludeExtraneousValues: true,
    });
  }

  // Add other fields and transformation logic as needed
}
